# 商品价格表分表实战验证文档

## 一、环境准备

### 1.1 数据库准备

#### 1.1.1 创建旧数据库

执行以下 SQL 脚本：

```bash
mysql -u root -p < doc/init_old_db.sql
```

脚本内容：
- 创建数据库 `price_db`
- 创建表 `product_price`（旧表）
- 创建表 `product_price_history`（历史表）

#### 1.1.2 创建新分片数据库

执行以下 SQL 脚本：

```bash
mysql -u root -p < doc/init_sharding_db.sql
```

脚本内容：
- 创建数据库 `price_db_0` ~ `price_db_3`
- 在每个数据库中创建分片表 `product_price_0` ~ `product_price_7`
- 在每个数据库中创建历史表 `product_price_history`

### 1.2 生成测试数据

#### 1.2.1 生成少量测试数据（用于快速验证）

```sql
USE price_db;

INSERT INTO product_price (product_id, sku_id, price, original_price, cost_price, status)
VALUES 
(1, 11, 99.99, 129.99, 59.99, 1),
(2, 21, 199.99, 259.99, 119.99, 1),
(3, 31, 299.99, 399.99, 179.99, 1),
(4, 41, 499.99, 699.99, 299.99, 1),
(5, 51, 599.99, 799.99, 359.99, 1);
```

#### 1.2.2 生成大量测试数据（用于性能测试）

```sql
USE price_db;

CALL generate_test_data(100000);
```

这将生成 10 万条测试数据。

#### 1.2.3 生成 3000 万+ 测试数据（用于完整测试）

```sql
USE price_db;

CALL generate_test_data(30000000);
```

**注意**：生成 3000 万数据需要较长时间，建议在非生产环境执行。

### 1.3 配置文件修改

根据实际数据库配置，修改以下配置文件：

#### 1.3.1 application-old.yml

```yaml
spring:
  shardingsphere:
    datasource:
      old-ds:
        url: jdbc:mysql://localhost:3306/price_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
        username: root
        password: 123456  # 修改为实际密码
```

#### 1.3.2 application-sharding.yml

```yaml
spring:
  shardingsphere:
    datasource:
      ds0:
        url: jdbc:mysql://localhost:3306/price_db_0?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
        username: root
        password: 123456  # 修改为实际密码
      ds1:
        url: jdbc:mysql://localhost:3306/price_db_1?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
        username: root
        password: 123456  # 修改为实际密码
      ds2:
        url: jdbc:mysql://localhost:3306/price_db_2?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
        username: root
        password: 123456  # 修改为实际密码
      ds3:
        url: jdbc:mysql://localhost:3306/price_db_3?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
        username: root
        password: 123456  # 修改为实际密码
```

#### 1.3.3 application-migration.yml

同样需要修改数据库连接信息。

## 二、启动应用

### 2.1 编译项目

```bash
cd sharding-jdbc-price
mvn clean package -DskipTests
```

### 2.2 启动应用（旧表模式）

```bash
java -jar target/sharding-jdbc-price-0.0.1-SNAPSHOT.jar --spring.profiles.active=old
```

### 2.3 验证应用启动成功

访问以下 URL，确认应用正常启动：

```bash
curl http://localhost:8087/migration/read-source
```

预期返回：
```json
{
  "success": true,
  "source": "old"
}
```

## 三、功能验证

### 3.1 旧表模式验证

#### 3.1.1 添加价格

```bash
curl -X POST http://localhost:8087/price/add \
  -H "Content-Type: application/json" \
  -d '{
    "productId": 100,
    "skuId": 1001,
    "price": 199.99,
    "originalPrice": 259.99,
    "costPrice": 119.99,
    "status": 1
  }'
```

预期返回：
```json
{
  "success": true,
  "message": "添加价格成功",
  "data": {
    "id": 1234567890123456789,
    "productId": 100,
    "skuId": 1001,
    "price": 199.99,
    "originalPrice": 259.99,
    "costPrice": 119.99,
    "status": 1,
    ...
  }
}
```

#### 3.1.2 查询当前价格

```bash
curl http://localhost:8087/price/current/100
```

预期返回：
```json
{
  "success": true,
  "data": {
    "id": 1234567890123456789,
    "productId": 100,
    "skuId": 1001,
    "price": 199.99,
    ...
  }
}
```

#### 3.1.3 查询 SKU 价格

```bash
curl http://localhost:8087/price/sku/1001
```

#### 3.1.4 按价格范围查询

```bash
curl "http://localhost:8087/price/range?productId=100&minPrice=100&maxPrice=300"
```

#### 3.1.5 查询历史价格

```bash
curl "http://localhost:8087/price/history/100?limit=5"
```

#### 3.1.6 更新价格

```bash
curl -X POST http://localhost:8087/price/update \
  -H "Content-Type: application/json" \
  -d '{
    "id": 1234567890123456789,
    "productId": 100,
    "skuId": 1001,
    "price": 179.99,
    "originalPrice": 259.99,
    "costPrice": 119.99,
    "status": 1
  }'
```

### 3.2 双写模式验证

#### 3.2.1 切换到迁移配置

停止应用，重新启动：

```bash
java -jar target/sharding-jdbc-price-0.0.1-SNAPSHOT.jar --spring.profiles.active=migration
```

#### 3.2.2 验证双写功能

添加价格：

```bash
curl -X POST http://localhost:8087/price/add \
  -H "Content-Type: application/json" \
  -d '{
    "productId": 200,
    "skuId": 2001,
    "price": 299.99,
    "originalPrice": 399.99,
    "costPrice": 179.99,
    "status": 1
  }'
```

#### 3.2.3 验证数据是否同时写入旧表和新表

查询旧表：

```sql
USE price_db;
SELECT * FROM product_price WHERE product_id = 200;
```

查询新表（根据 product_id % 4 和 product_id % 8 计算表名）：

```sql
-- product_id = 200
-- 200 % 4 = 0 -> ds0
-- 200 % 8 = 0 -> product_price_0
USE price_db_0;
SELECT * FROM product_price_0 WHERE product_id = 200;
```

两个表都应该有相同的数据。

### 3.3 数据迁移验证

#### 3.3.1 全量数据迁移

```bash
curl -X POST http://localhost:8087/migration/migrate-all
```

预期返回：
```json
{
  "success": true,
  "message": "全量数据迁移完成",
  "failedCount": 0
}
```

#### 3.3.2 并发数据迁移

```bash
curl -X POST http://localhost:8087/migration/migrate-concurrent
```

#### 3.3.3 验证迁移数据量

查询旧表总数：

```sql
USE price_db;
SELECT COUNT(*) FROM product_price;
```

查询新表总数（需要查询所有分片表）：

```sql
SELECT SUM(cnt) FROM (
    SELECT COUNT(*) AS cnt FROM price_db_0.product_price_0
    UNION ALL
    SELECT COUNT(*) FROM price_db_0.product_price_1
    UNION ALL
    SELECT COUNT(*) FROM price_db_0.product_price_2
    UNION ALL
    SELECT COUNT(*) FROM price_db_0.product_price_3
    UNION ALL
    SELECT COUNT(*) FROM price_db_0.product_price_4
    UNION ALL
    SELECT COUNT(*) FROM price_db_0.product_price_5
    UNION ALL
    SELECT COUNT(*) FROM price_db_0.product_price_6
    UNION ALL
    SELECT COUNT(*) FROM price_db_0.product_price_7
    UNION ALL
    SELECT COUNT(*) FROM price_db_1.product_price_0
    UNION ALL
    SELECT COUNT(*) FROM price_db_1.product_price_1
    UNION ALL
    SELECT COUNT(*) FROM price_db_1.product_price_2
    UNION ALL
    SELECT COUNT(*) FROM price_db_1.product_price_3
    UNION ALL
    SELECT COUNT(*) FROM price_db_1.product_price_4
    UNION ALL
    SELECT COUNT(*) FROM price_db_1.product_price_5
    UNION ALL
    SELECT COUNT(*) FROM price_db_1.product_price_6
    UNION ALL
    SELECT COUNT(*) FROM price_db_1.product_price_7
    UNION ALL
    SELECT COUNT(*) FROM price_db_2.product_price_0
    UNION ALL
    SELECT COUNT(*) FROM price_db_2.product_price_1
    UNION ALL
    SELECT COUNT(*) FROM price_db_2.product_price_2
    UNION ALL
    SELECT COUNT(*) FROM price_db_2.product_price_3
    UNION ALL
    SELECT COUNT(*) FROM price_db_2.product_price_4
    UNION ALL
    SELECT COUNT(*) FROM price_db_2.product_price_5
    UNION ALL
    SELECT COUNT(*) FROM price_db_2.product_price_6
    UNION ALL
    SELECT COUNT(*) FROM price_db_2.product_price_7
    UNION ALL
    SELECT COUNT(*) FROM price_db_3.product_price_0
    UNION ALL
    SELECT COUNT(*) FROM price_db_3.product_price_1
    UNION ALL
    SELECT COUNT(*) FROM price_db_3.product_price_2
    UNION ALL
    SELECT COUNT(*) FROM price_db_3.product_price_3
    UNION ALL
    SELECT COUNT(*) FROM price_db_3.product_price_4
    UNION ALL
    SELECT COUNT(*) FROM price_db_3.product_price_5
    UNION ALL
    SELECT COUNT(*) FROM price_db_3.product_price_6
    UNION ALL
    SELECT COUNT(*) FROM price_db_3.product_price_7
) AS total;
```

两个总数应该相等。

### 3.4 数据校验验证

#### 3.4.1 全量数据校验

```bash
curl -X POST http://localhost:8087/migration/check-all
```

预期返回：
```json
{
  "success": true,
  "message": "全量数据校验完成"
}
```

查看应用日志，确认没有数据不一致的错误。

#### 3.4.2 抽样校验

```bash
curl -X POST http://localhost:8087/migration/check-random
```

### 3.5 流量切换验证

#### 3.5.1 切换读流量到新表

```bash
curl -X POST "http://localhost:8087/migration/switch-read?source=new"
```

预期返回：
```json
{
  "success": true,
  "message": "读流量切换成功",
  "source": "new"
}
```

#### 3.5.2 验证读源

```bash
curl http://localhost:8087/migration/read-source
```

预期返回：
```json
{
  "success": true,
  "source": "new"
}
```

#### 3.5.3 验证查询功能

```bash
curl http://localhost:8087/price/current/100
```

确认查询结果正确。

#### 3.5.4 切换回旧表

```bash
curl -X POST "http://localhost:8087/migration/switch-read?source=old"
```

#### 3.5.5 双读对比模式

```bash
curl -X POST "http://localhost:8087/migration/switch-read?source=both"
```

在双读对比模式下，系统会同时查询旧表和新表，并对比数据一致性。

### 3.6 分片表模式验证

#### 3.6.1 切换到分片配置

停止应用，重新启动：

```bash
java -jar target/sharding-jdbc-price-0.0.1-SNAPSHOT.jar --spring.profiles.active=sharding
```

#### 3.6.2 验证分片查询

添加价格：

```bash
curl -X POST http://localhost:8087/price/add \
  -H "Content-Type: application/json" \
  -d '{
    "productId": 300,
    "skuId": 3001,
    "price": 399.99,
    "originalPrice": 499.99,
    "costPrice": 239.99,
    "status": 1
  }'
```

#### 3.6.3 验证数据分布

查询数据应该路由到正确的分片表：

```sql
-- product_id = 300
-- 300 % 4 = 0 -> ds0
-- 300 % 8 = 4 -> product_price_4
USE price_db_0;
SELECT * FROM product_price_4 WHERE product_id = 300;
```

#### 3.6.4 验证查询性能

使用 Apache Bench 进行压力测试：

```bash
ab -n 10000 -c 100 http://localhost:8087/price/current/300
```

对比旧表和分片表的查询性能。

## 四、性能测试

### 4.1 查询性能测试

#### 4.1.1 旧表查询性能

```bash
ab -n 10000 -c 100 http://localhost:8087/price/current/100
```

记录 QPS、平均响应时间等指标。

#### 4.1.2 分片表查询性能

切换到分片模式后，执行相同测试：

```bash
ab -n 10000 -c 100 http://localhost:8087/price/current/100
```

对比性能提升。

### 4.2 写入性能测试

#### 4.2.1 旧表写入性能

```bash
for i in {1..1000}; do
  curl -X POST http://localhost:8087/price/add \
    -H "Content-Type: application/json" \
    -d "{
      \"productId\": $((1000 + i)),
      \"skuId\": $((10000 + i)),
      \"price\": 199.99,
      \"originalPrice\": 259.99,
      \"costPrice\": 119.99,
      \"status\": 1
    }" &
done
wait
```

#### 4.2.2 分片表写入性能

切换到分片模式后，执行相同测试，对比性能。

### 4.3 并发测试

使用 JMeter 或其他工具进行并发测试：

1. 设置 100 个并发用户
2. 每个用户执行 100 次查询
3. 记录 QPS、响应时间、错误率等指标

## 五、异常场景验证

### 5.1 数据库连接失败

1. 停止一个数据库实例
2. 执行查询操作
3. 验证系统是否能正常降级

### 5.2 数据不一致

1. 手动修改旧表数据
2. 使用双读对比模式查询
3. 验证是否能检测到数据不一致

### 5.3 迁移失败

1. 在迁移过程中断开数据库连接
2. 验证是否能记录失败数据
3. 验证重试机制是否正常

### 5.4 回滚验证

1. 切换到新表模式
2. 发现问题后切换回旧表
3. 验证数据是否完整

## 六、验证清单

### 6.1 功能验证清单

- [ ] 旧表模式下添加价格
- [ ] 旧表模式下查询价格
- [ ] 旧表模式下更新价格
- [ ] 旧表模式下按价格范围查询
- [ ] 旧表模式下查询历史价格
- [ ] 双写模式下数据同时写入旧表和新表
- [ ] 全量数据迁移
- [ ] 并发数据迁移
- [ ] 全量数据校验
- [ ] 抽样数据校验
- [ ] 读流量切换到新表
- [ ] 读流量切换到旧表
- [ ] 双读对比模式
- [ ] 分片表模式下添加价格
- [ ] 分片表模式下查询价格
- [ ] 分片表模式下更新价格
- [ ] 数据正确路由到分片表

### 6.2 性能验证清单

- [ ] 旧表查询性能测试
- [ ] 分片表查询性能测试
- [ ] 旧表写入性能测试
- [ ] 分片表写入性能测试
- [ ] 并发测试
- [ ] 性能对比分析

### 6.3 异常场景验证清单

- [ ] 数据库连接失败
- [ ] 数据不一致检测
- [ ] 迁移失败处理
- [ ] 回滚功能验证

### 6.4 数据一致性验证清单

- [ ] 旧表数据总量 = 新表数据总量
- [ ] 随机抽样数据一致性
- [ ] 全量数据校验
- [ ] 双读对比验证

## 七、验证报告模板

### 7.1 验证环境

- 操作系统：
- Java 版本：
- MySQL 版本：
- 数据量：
- 测试时间：

### 7.2 功能验证结果

| 功能模块 | 测试项 | 测试结果 | 备注 |
|---------|-------|---------|------|
| 旧表模式 | 添加价格 | 通过/失败 | |
| 旧表模式 | 查询价格 | 通过/失败 | |
| 双写模式 | 双写功能 | 通过/失败 | |
| 数据迁移 | 全量迁移 | 通过/失败 | |
| 数据校验 | 全量校验 | 通过/失败 | |
| 流量切换 | 切换读源 | 通过/失败 | |
| 分片表模式 | 添加价格 | 通过/失败 | |
| 分片表模式 | 查询价格 | 通过/失败 | |

### 7.3 性能验证结果

| 测试项 | 旧表 QPS | 分片表 QPS | 性能提升 |
|-------|---------|-----------|---------|
| 查询性能 | | | |
| 写入性能 | | | |

### 7.4 数据一致性验证结果

| 验证项 | 旧表数据量 | 新表数据量 | 一致性 |
|-------|-----------|-----------|-------|
| 数据总量 | | | |
| 抽样校验 | | | |
| 全量校验 | | | |

### 7.5 问题记录

| 序号 | 问题描述 | 严重程度 | 处理状态 |
|-----|---------|---------|---------|
| 1 | | | |
| 2 | | | |

### 7.6 验证结论

- 功能验证：通过/不通过
- 性能验证：通过/不通过
- 数据一致性：通过/不通过
- 总体评价：

## 八、注意事项

1. **数据备份**：在执行迁移前，务必备份旧表数据
2. **测试环境**：建议先在测试环境充分验证，再在生产环境执行
3. **监控告警**：迁移过程中密切监控系统指标
4. **回滚方案**：准备好回滚方案，以便快速响应问题
5. **灰度发布**：采用灰度发布策略，逐步切换流量
6. **数据校验**：迁移完成后，务必进行完整的数据校验
7. **性能测试**：进行充分的性能测试，确保分片后性能满足要求

## 九、常见问题

### Q1: 迁移过程中应用是否需要停机？

A: 不需要。通过双写机制，应用可以持续提供服务。

### Q2: 如何确保数据一致性？

A: 通过双写、数据校验、双读对比等多种机制确保数据一致性。

### Q3: 迁移失败如何处理？

A: 系统会记录失败的数据，支持重试。同时可以切换回旧表，保证业务正常运行。

### Q4: 性能提升多少？

A: 根据实际测试，查询性能可提升 80% 以上，具体取决于数据量和查询模式。

### Q5: 如何选择分片键？

A: 选择查询频率高、数据分布均匀、不常更新的字段作为分片键。