# 商品价格表分表实战实现细节文档

## 一、项目概述

### 1.1 场景描述
某电商平台商品价格系统，商品价格表（product_price）已有 3000 万+ 数据，单表查询性能下降严重，需要使用 Sharding-JDBC 进行分表优化。

### 1.2 核心需求
1. 价格系统不能停服，需要在线迁移
2. 查询场景：按商品 ID 查询价格、按 SKU ID 查询价格、按价格范围查询
3. 写入场景：价格更新频繁，需要保证数据一致性
4. 历史数据查询：需要查询历史价格记录

### 1.3 技术选型
- **分片框架**：Sharding-JDBC 4.0.0-RC1
- **数据库**：MySQL 5.7+
- **ORM框架**：MyBatis
- **连接池**：Druid

## 二、分表方案设计

### 2.1 分片策略

采用**按商品 ID 分片**的策略，原因如下：
1. 按商品 ID 查询是最常见的场景，性能最优
2. 数据分布均匀，避免数据倾斜
3. 配置简单，维护成本低
4. 适合价格更新频繁的场景

### 2.2 分片规则

**分库规则**：4个数据库，按 product_id % 4 分库
**分表规则**：每个数据库8张表，按 product_id % 8 分表

**数据节点**：ds$->{0..3}.product_price_$->{0..7}

共 4 × 8 = 32 张物理表

### 2.3 分片算法

使用 Inline 表达式实现分片算法：

```yaml
# 分库算法
database-strategy:
  inline:
    sharding-column: product_id
    algorithm-expression: ds$->{product_id % 4}

# 分表算法
table-strategy:
  inline:
    sharding-column: product_id
    algorithm-expression: product_price_$->{product_id % 8}
```

### 2.4 主键生成策略

使用 SNOWFLAKE 算法生成分布式主键：

```yaml
key-generator:
  column: id
  type: SNOWFLAKE
  props:
    worker:
      id: 1
```

## 三、数据库设计

### 3.1 表结构

**商品价格表（product_price）**

```sql
CREATE TABLE product_price (
    id BIGINT PRIMARY KEY,
    product_id BIGINT NOT NULL COMMENT '商品ID',
    sku_id BIGINT NOT NULL COMMENT 'SKU ID',
    price DECIMAL(10,2) NOT NULL COMMENT '价格',
    original_price DECIMAL(10,2) COMMENT '原价',
    cost_price DECIMAL(10,2) COMMENT '成本价',
    start_time DATETIME COMMENT '生效开始时间',
    end_time DATETIME COMMENT '生效结束时间',
    status TINYINT DEFAULT 1 COMMENT '状态：1-有效，0-无效',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_product_id (product_id),
    INDEX idx_sku_id (sku_id),
    INDEX idx_price (price),
    INDEX idx_create_time (create_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品价格表';
```

**商品价格历史表（product_price_history）**

```sql
CREATE TABLE product_price_history (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    price_id BIGINT NOT NULL COMMENT '原价格表ID',
    product_id BIGINT NOT NULL COMMENT '商品ID',
    sku_id BIGINT NOT NULL COMMENT 'SKU ID',
    price DECIMAL(10,2) NOT NULL COMMENT '价格',
    original_price DECIMAL(10,2) COMMENT '原价',
    cost_price DECIMAL(10,2) COMMENT '成本价',
    start_time DATETIME COMMENT '生效开始时间',
    end_time DATETIME COMMENT '生效结束时间',
    status TINYINT DEFAULT 1 COMMENT '状态',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_product_id (product_id),
    INDEX idx_sku_id (sku_id),
    INDEX idx_price_id (price_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品价格历史表';
```

### 3.2 数据库分布

**旧数据库**：
- price_db：包含单表 product_price

**新数据库（分片）**：
- price_db_0：product_price_0 ~ product_price_7
- price_db_1：product_price_0 ~ product_price_7
- price_db_2：product_price_0 ~ product_price_7
- price_db_3：product_price_0 ~ product_price_7

## 四、核心功能实现

### 4.1 实体类

**ProductPrice.java**

```java
@Data
public class ProductPrice {
    private Long id;
    private Long productId;
    private Long skuId;
    private BigDecimal price;
    private BigDecimal originalPrice;
    private BigDecimal costPrice;
    private Date startTime;
    private Date endTime;
    private Integer status;
    private Date createTime;
    private Date updateTime;
}
```

**ProductPriceHistory.java**

```java
@Data
public class ProductPriceHistory {
    private Long id;
    private Long priceId;
    private Long productId;
    private Long skuId;
    private BigDecimal price;
    private BigDecimal originalPrice;
    private BigDecimal costPrice;
    private Date startTime;
    private Date endTime;
    private Integer status;
    private Date createTime;
    private Date updateTime;
}
```

### 4.2 Mapper 接口

**ProductPriceMapper.java**

提供以下方法：
- `insert(ProductPrice productPrice)` - 插入价格
- `update(ProductPrice productPrice)` - 更新价格
- `selectById(Long id)` - 根据ID查询
- `selectByProductId(Long productId)` - 根据商品ID查询当前价格
- `selectBySkuId(Long skuId)` - 根据SKU ID查询当前价格
- `selectByPriceRange(...)` - 按价格范围查询
- `selectHistoryByProductId(...)` - 查询历史价格
- `count()` - 统计总数
- `selectByPage(...)` - 分页查询
- `deleteById(Long id)` - 删除价格

### 4.3 Service 层

**ProductPriceService.java**

提供基础的价格管理功能：
- `addPrice(ProductPrice productPrice)` - 添加价格
- `updatePrice(ProductPrice productPrice)` - 更新价格
- `getCurrentPrice(Long productId)` - 查询当前价格
- `getSkuPrice(Long skuId)` - 查询SKU价格
- `queryByPriceRange(...)` - 按价格范围查询
- `queryHistoryPrice(...)` - 查询历史价格

**ProductPriceMigrationService.java**

实现双写逻辑：
- `insertWithDualWrite(ProductPrice productPrice)` - 双写插入
- `updateWithDualWrite(ProductPrice productPrice)` - 双写更新

**ProductPriceReadService.java**

实现读写分离逻辑：
- `selectByProductId(Long productId)` - 根据配置选择读源
- `selectById(Long id)` - 根据配置选择读源
- 支持双读对比模式

**ProductPriceMigrationJob.java**

实现数据迁移功能：
- `migrateAllData()` - 全量数据迁移
- `migrateDataConcurrently()` - 并发数据迁移

**DataConsistencyChecker.java**

实现数据校验功能：
- `checkAllData()` - 全量数据校验
- `randomCheck()` - 抽样校验

### 4.4 Controller 层

**ProductPriceController.java**

提供价格管理 API：
- `POST /price/add` - 添加价格
- `POST /price/update` - 更新价格
- `GET /price/current/{productId}` - 查询当前价格
- `GET /price/sku/{skuId}` - 查询SKU价格
- `GET /price/range` - 按价格范围查询
- `GET /price/history/{productId}` - 查询历史价格

**MigrationController.java**

提供迁移管理 API：
- `POST /migration/switch-read` - 切换读流量
- `GET /migration/read-source` - 获取当前读源
- `POST /migration/migrate-all` - 全量数据迁移
- `POST /migration/migrate-concurrent` - 并发数据迁移
- `POST /migration/check-all` - 全量数据校验
- `POST /migration/check-random` - 抽样校验

## 五、配置文件

### 5.1 application.yml

主配置文件，包含：
- 服务端口：8087
- 默认激活配置：old
- 迁移配置开关

### 5.2 application-old.yml

旧表配置，用于迁移前：
- 数据源：old-ds（price_db）
- 不分片，使用单表

### 5.3 application-sharding.yml

分片表配置，用于迁移后：
- 数据源：ds0, ds1, ds2, ds3
- 分片规则：按 product_id 分库分表
- 主键生成：SNOWFLAKE

### 5.4 application-migration.yml

迁移配置，用于迁移期间：
- 数据源：old-ds + ds0, ds1, ds2, ds3
- 双写开关：enabled
- 读源配置：old（可动态切换）

## 六、迁移方案

### 6.1 迁移流程

**阶段一：准备阶段（1-2天）**
1. 创建新数据库和分片表
2. 配置 Sharding-JDBC
3. 测试环境验证

**阶段二：双写阶段（3-5天）**
1. 实现双写逻辑
2. 配置双写开关
3. 实现读写分离逻辑
4. 灰度发布双写功能

**阶段三：历史数据迁移（5-7天）**
1. 全量数据迁移
2. 增量数据同步（基于 Binlog）
3. 迁移进度监控

**阶段四：数据校验（2-3天）**
1. 全量数据校验
2. 抽样校验
3. 数据一致性检查

**阶段五：流量切换（1-2天）**
1. 切换 10% 读流量到新表
2. 切换 50% 读流量到新表
3. 切换 100% 读流量到新表
4. 停止双写，只写新表

**阶段六：清理阶段（1天）**
1. 下线旧表
2. 清理旧数据
3. 归档历史数据

### 6.2 双写实现

```java
@Transactional
public void insertWithDualWrite(ProductPrice productPrice) {
    productPrice.setCreateTime(new Date());
    productPrice.setUpdateTime(new Date());
    productPrice.setStatus(1);
    
    if (dualWriteEnabled && oldTableWriteEnabled) {
        oldPriceMapper.insert(productPrice);
    }
    
    if (dualWriteEnabled && newTableWriteEnabled) {
        newPriceMapper.insert(productPrice);
    }
    
    ProductPriceHistory history = new ProductPriceHistory();
    BeanUtils.copyProperties(productPrice, history);
    history.setPriceId(productPrice.getId());
    historyMapper.insert(history);
}
```

### 6.3 数据迁移实现

**全量迁移**：
- 分页查询旧表数据
- 批量插入新表
- 记录失败数据，支持重试

**并发迁移**：
- 使用线程池并发迁移
- 提高迁移效率
- 控制并发度，避免数据库压力过大

### 6.4 数据校验实现

**全量校验**：
- 逐条对比旧表和新表数据
- 记录不一致数据
- 生成校验报告

**抽样校验**：
- 随机抽取样本数据
- 对比数据一致性
- 快速验证迁移质量

## 七、监控与告警

### 7.1 监控指标

- 迁移进度
- 数据不一致数
- 查询延迟
- 系统性能指标

### 7.2 告警规则

- 迁移进度停滞告警
- 数据不一致率过高告警
- 查询延迟过高告警

## 八、风险控制

### 8.1 回滚方案

1. 切换读流量到旧表
2. 停止双写
3. 发送告警通知

### 8.2 数据备份

1. 迁移前备份旧表
2. 备份 Binlog
3. 定期备份新表

### 8.3 压力测试

1. 模拟高并发场景
2. 测试系统性能
3. 验证数据一致性

## 九、优化建议

### 9.1 性能优化

- 使用批量插入，减少网络开销
- 合理设置连接池大小
- 添加合适的索引，提高查询性能
- 使用读写分离，减轻主库压力

### 9.2 监控优化

- 实时监控迁移进度
- 监控数据一致性
- 监控系统性能指标
- 设置合理的告警阈值

### 9.3 安全优化

- 数据备份
- 回滚方案
- 灰度发布
- 压力测试

## 十、总结

### 10.1 核心要点

1. **分片策略**：按商品 ID 分片，适合高频查询场景
2. **迁移方案**：双写 + 增量同步 + 数据校验 + 流量切换
3. **不停服**：通过双写和灰度切换实现平滑迁移
4. **数据一致性**：通过 Binlog 同步和数据校验保证
5. **风险控制**：完善的回滚方案和监控告警

### 10.2 注意事项

- 充分测试，确保方案可行
- 监控到位，及时发现问题
- 准备回滚方案，降低风险
- 灰度发布，逐步切换流量
- 文档完善，便于后续维护

### 10.3 预期效果

- 查询性能提升 80%+
- 支持更高并发
- 数据分布均匀
- 系统稳定性提升